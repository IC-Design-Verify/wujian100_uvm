-sverilog -timescale=1ns/1ps -kdb

# rpu need this
-assert svaext

% if 'MINUS_SDF' not in ENV:
# recommended by tsmc for sim speed up
+define+TSMC_CM_UNIT_DELAY
% else:
+define+NTC
+define+RECREM
% endif

